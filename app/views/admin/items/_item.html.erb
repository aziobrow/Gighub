  <tr>
    <td><%= image_tag(item.image_url, size: "100", alt: item.title, class: "card-img-top") %></td>
    <td><%= link_to "#{item.title}", item_path(item) %></td>
    <td><%= item.description %></td>
    <td>
      <% if item.retired? %>
        <em>RETIRED</em>
      <% else %>
        ACTIVE
      <% end %>
    </td>
    <td><%= link_to "Edit", edit_admin_item_path(item) %></td>
  </tr>
